services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: online-news-api
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - online-news-network

networks:
  online-news-network:
    driver: bridge
